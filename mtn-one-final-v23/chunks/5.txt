tor]');
const slider=root.querySelector('[data-tier-slider]');
const exact=root.querySelector('[data-exact]');
const periodBtns=[...root.querySelectorAll('[data-period]')];
const cards=document.querySelector('[data-tier-cards]');
const tbody=document.querySelector('[data-table-body]');
const result=root.querySelector('.result');
const stickyMtu=document.querySelector('[data-sticky-mtu]');
const stickyPrice=document.querySelector('[data-sticky-price]');
const stickyMode=document.querySelector('[data-sticky-mode]');
const q=new URLSearchParams(location.search);
let period=q.get('period')==='annual'?'annual':'monthly';
let requested=Number(q.get('mtu'));
let exactMtu=Number.isFinite(requested)&&requested>0?Math.min(40,requested)*1e6:8e6;
let activeIndex=tierIndexForMtu(exactMtu);
let previousMode=null;
function amount(n){return fmtUsd(period==='annual'?n*12:n)}
function syncUrl(){try{const u=new URL(location.href);u.searchParams.set('mtu',String(Number((exactMtu/1e6).toFixed(1))));u.searchParams.set('period',period);history.replaceState(null,'',u)}catch(e){}}
function midpointFor(i){const low=i===0?0:tiers[i-1].max+1;return Math.round((low+tiers[i].max)/2)}
function buildSchedule(){cards.innerHTML='';tbody.innerHTML='';tiers.forEach((t,i)=>{const alpha=t.mode==='alpha';const committed=alpha?t.fee*.8:t.fee;const risk=alpha?t.fee*.2:0;const c=document.createElement('button');c.type='button';c.className=`tier-card ${alpha?'alpha':'fixed'}`;c.dataset.index=i;c.setAttribute('aria-label',`${t.label}, ${amount(t.fee)} ${alpha?'maximum fee with Alpha 80/20':'monthly fee'}`);c.innerHTML=`<div class="tier">${t.label}</div><div class="price">${amount(t.fee)}</div><div class="kind">${alpha?'Alpha 80/20':'Fixed pricing'}</div>`;c.addEventListener('click',()=>render(i,midpointFor(i),true));cards.appendChild(c);const tr=document.createElement('tr');tr.dataset.index=i;tr.innerHTML=`<th>${t.label}</th><td class="money">${amount(t.fee)}</td><td><span class="pill ${t.mode}">${alpha?'Alpha 80/20':'Fixed'}</span></td><td class="money">${amount(committed)}</td><td>${alpha?'Up to '+amount(risk):'—'}</td>`;tbody.appendChild(tr);});}
function animateModeShift(mode){if(previousMode&&previousMode!==mode){const toast=root.querySelector('[data-mode-transition]');toast.textContent=mode==='alpha'?'Alpha activated · shared success':'Fixed pricing · predictable launch';result.classList.remove('mode-shift');void result.offsetWidth;result.classList.add('mode-shift');setTimeout(()=>result.classList.remove('mode-shift'),1200)}previousMode=mode}
function render(index,mtu,updateUrl=true){activeIndex=Math.max(0,Math.min(9,Number(index)||0));slider.value=String(activeIndex);const t=tiers[activeIndex];exactMtu=Math.max(activeIndex?tiers[activeIndex-1].max+1:0,Math.min(t.max,mtu??t.max));const alpha=t.mode==='alpha';const mult=period==='annual'?12:1;const fee=t.fee*mult,committed=(alpha?t.fee*.8:t.fee)*mult,risk=(alpha?t.fee*.2:0)*mult;animateModeShift(t.mode);result.classList.toggle('alpha-result',alpha);result.classList.toggle('fixed-result',!alpha);const feeEl=root.querySelector('[data-fee]');feeEl.textContent=fmtUsd(fee);feeEl.classList.remove('price-jump');void feeEl.offsetWidth;feeEl.classList.add('price-jump');root.querySelector('[data-mode]').textContent=alpha?'Alpha Growth Pricing':'Fixed pricing';root.querySelector('[data-mode]').classList.toggle('fixed',!alpha);root.querySelector('[data-band]').textContent=`${t.label} MTUs`;root.querySelector('[data-fee-label]').textContent=alpha?(period==='annual'?'Maximum annualised fee':'Maximum monthly fee'):(period==='annual'?'Annualised run rate':'Monthly fee');root.querySelector('[data-period-label]').textContent=period==='annual'?'12× monthly run rate · USD':'per month · USD';root.querySelector('[data-selected]').textContent=`${fmtMtu(exactMtu)} MTUs selected`;root.querySelector('[data-committed-label]').textContent=alpha?'80% committed':'100% committed';root.querySelector('[data-committed]').textContent=fmtUsd(committed);root.querySelector('[data-risk-label]').textContent=alpha?'Up to 20% KPI-linked':'KPI-linked';root.querySelector('[data-risk]').textContent=alpha?'up to '+fmtUsd(risk):'Not applicable';root.querySelector('[data-committed-bar]').style.width=alpha?'80%':'100%';root.querySelector('[data-risk-bar]').style.width=alpha?'20%':'0%';root.querySelector('[data-skin]').innerHTML=alpha?`<strong>Netcore has skin in the game.</strong> Up to ${fmtUsd(risk)} of Netcore's ${period==='annual'?'annualised':'monthly'} fee is earned against jointly agreed KPIs.`:`<strong>Predictable launch economics.</strong> The monthly fee is fully committed through 5 million MTUs; Alpha shared-success pricing begins above 5 million.`;exact.value=(exactMtu/1e6).toFixed(exactMtu%1e6===0?0:1);root.querySelector('[data-slider-band]').textContent=`${t.label} · ${fmtMtu(exactMtu)} MTUs`;if(stickyMtu)stickyMtu.textContent=`${fmtMtu(exactMtu)} MTUs`;if(stickyPrice)stickyPrice.textContent=fmtUsd(fee);if(stickyMode)stickyMode.textContent=alpha?`Alpha 80/20 · ${period==='annual'?'maximum annualised fee':'maximum monthly fee'}`:`Fixed · ${period==='annual'?'annualised run rate':'monthly fee'}`;document.querySelectorAll('[data-index]').forEach(el=>{const selected=Number(el.dataset.index)===activeIndex;el.classList.toggle('active',selected);if(el.matches('button.tier-card'))el.setAttribute('aria-pressed',selected?'true':'false')});if(updateUrl)syncUrl()}
slider.addEventListener('input',()=>render(Number(slider.value),midpointFor(Number(slider.value)),true));slider.addEventListener('change',()=>slider.dispatchEvent(new Event('input')));
exact.addEventListener('focus',()=>exact.select());exact.addEventListener('change',()=>{const n=parseMtu(exact.value);render(tierIndexForMtu(n),n,true)});exact.addEventListener('keydown',e=>{if(e.key==='Enter'){exact.blur();exact.dispatchEvent(new Event('change'))}});
periodBtns.forEach(b=>b.addEventListener('click',()=>{period=b.dataset.period;periodBtns.forEach(x=>x.classList.toggle('active',x===b));buildSchedule();render(activeIndex,exactMtu,true)}));
periodBtns.forEach(b=>b.classList.toggle('active',b.dataset.period===period));
function wirePopover(wrapSel,btnSel){const w=document.querySelector(wrapSel),b=w?.querySelector(btnSel);if(!w||!b)return;b.addEventListener('click',e=>{e.stopPropagation();const open=w.classList.toggle('open');b.setAttribute('aria-expanded',String(open))});document.addEventListener('click',e=>{if(!w.contains(e.target)){w.classList.remove('open');b.setAttribute('aria-expanded','false')}});document.addEventListener('keydown',e=>{if(e.key==='Escape'){w.classList.remove('open');b.setAttribute('aria-expanded','false')}})}
wirePopover('[data-info-wrap]','.info-btn');wirePopover('[data-annual-info]','.annual-info-btn');

// V22 mobile/touch delegation fallback: ensures every pricing card changes calculator state even if a browser suppresses an individual click handler.
cards.addEventListener('pointerup',e=>{const c=e.target.closest('button.tier-card');if(!c)return;const i=Number(c.dataset.index);if(Number.isFinite(i))render(i,midpointFor(i),true)});
cards.addEventListener('touchend',e=>{const c=e.target.closest('button.tier-card');if(!c)return;const i=Number(c.dataset.index);if(Number.isFinite(i))requestAnimationFrame(()=>render(i,midpointFor(i),true))},{passive:true});

const menuBtn=document.querySelector('.menu');const nav=document.querySelector('.nav');
if(menuBtn&&nav){if(!nav.id)nav.id='primary-nav';menuBtn.setAttribute('aria-controls',nav.id);const closeMenu=()=>{nav.classList.remove('open');menuBtn.setAttribute('aria-expanded','false')};menuBtn.addEventListener('click',e=>{e.stopPropagation();const open=nav.classList.toggle('open');menuBtn.setAttribute('aria-expanded',String(open))});nav.addEventListener('click',e=>{if(e.target.closest('a'))closeMenu()});document.addEventListener('click',e=>{if(nav.classList.contains('open')&&!nav.contains(e.target)&&e.target!==menuBtn)closeMenu()});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeMenu()})}
buildSchedule();render(activeIndex,exactMtu,false);syncUrl();
})();
</script>
</body></html>